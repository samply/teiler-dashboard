version: "3"

services:

  teiler-dashboard:
#    image: teiler-dashboard
    build: .

#    depends_on:
#      - teiler-core
    ports:
      - 4200:80
    environment:
      DEFAULT_LANGUAGE: "DE"
      TEILER_CORE_URL: "http://localhost:8085"
      KEYCLOAK_URL: "http://localhost:8380/login"
      KEYCLOAK_REALM: "teiler-dashboard"
      KEYCLOAK_CLIENT_ID: "teiler-dashboard"
      TEILER_ADMIN_NAME: "Max Mustermann"
      TEILER_ADMIN_EMAIL: "max.mustermann@teiler-example.com"
      TEILER_ADMIN_PHONE: "+49 123 456789"
      TEILER_PROJECT: "DKTK"
      TEILER_UI_SERVER_NAME: "localhost"
      TEILER_ROOT_CONFIG_URL: "http://localhost:9000"
      TEILER_UI_HTTP_RELATIVE_PATH: ""
      TEILER_ROOT_CONFIG_HTTP_RELATIVE_PATH: ""

#  teiler-core :
#    image: teiler-core
